[build]
  base    = "newsdetection"
  publish = "newsdetection/dist"
  command = "npm run build"

[functions]
  directory = "newsdetection/netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# SPA client-side routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
