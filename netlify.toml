[build]
  base    = "newsdetection"
  publish = "newsdetection/dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# SPA client-side routing
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
