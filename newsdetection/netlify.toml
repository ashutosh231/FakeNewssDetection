[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# SPA client-side routing — serve index.html for all paths
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
